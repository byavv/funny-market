{
  "name": "strong-gateway",
  "version": "0.0.1",
  "description": "strongloop jwt-based api gateway",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd servers/gate && npm install && cd ../api && npm install && cd ../.."
  },
  "keywords": [
    "gateway",
    "api",
    "authentication",
    "loopback"
  ],
  "author": "aksenchyk <aksenchyk.v@gmail.com>",
  "license": "ISC",
  "dependencies": {}
}
